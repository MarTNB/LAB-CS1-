#include <stdio.h>
int main()
{
    double a,b,sub,sum,multiplication,division;

    printf("Give the First valu\n");

    scanf("%lf", &a);

    printf("Give the secound valu\n");

    scanf("%lf", &b);

    printf("The sum of numbers %lf is %lf",a+b,sum);



    printf("The subtraction of numbers %lf  is %lf",a-b,sub);


    printf("The multiplication of numbers %lf  is %lf",a*b,multiplication);


    printf("The division of numbers %lf  is %lf",a/b,division);

    return 0;
}
